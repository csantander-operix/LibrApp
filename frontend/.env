# URL base del backend FastAPI.
# En dev se deja SIN definir a propósito: el frontend deriva la API del host desde
# el que se abre (localhost en la PC, o la IP de la PC desde el celular en la red).
# En producción se setea explícita.
# VITE_API_URL=http://localhost:8000/api/v1
